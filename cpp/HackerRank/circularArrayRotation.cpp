#include <bits/stdc++.h>
using namespace std;

int main(){
	// #ifndef ONLINE_JUDGE
	// freopen("input.txt", "r", stdin);
	// freopen("output.txt", "w", stdout);
	// #endif

	// int n, k, q; cin >>n >>k >>q;
	// int arr[n+k-1] = {0};

	// for (int i=0; i<n; i++) cin>> arr[n-1+i];
	// // for (int i=0; i<n+k; i++) cout << arr[i] << " ";
	// // cout << endl;

	// int t =k;
	// for (int i=1; i<=t; i++) k--, swap(arr[k], arr[n+t-i]);
	// // for (int i=0; i<n+k; i++) cout << arr[i] << " ";
	// int x;
	// for (int i=0; i<q; i++) cin>> x, cout << arr[x] << endl;
	// int arr[5] = {1, 2, 3, 4, 5};
	cout << arr[--];
}
